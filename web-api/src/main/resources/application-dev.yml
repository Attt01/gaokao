
env: DEV
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gaokao?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2B8
    username: Gaokao
    password: Gaokao522
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: o2o
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
wx:
  #支付
  pay:
    # 小程序appid
    appId: wxfd24dcdd82129aaa
    # 商户号
    mchId: 1613925940
    # api密钥
    mchKey: Ljmswxfd24dcdd82129aaa0123456789
    # 证书路径
    keyPath: classpath:cert/apiclient_cert.p12
    # 微信支付异步回调地址
    payCallbackUrl:
    sanbox: true


server:
  port: 9090

